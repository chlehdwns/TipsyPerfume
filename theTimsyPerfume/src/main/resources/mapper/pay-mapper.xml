<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="payMapper">
	
	
	
	<insert id="insertEmailPayKakao" parameterType="string">
		INSERT INTO TB_PAY_KAKAO(PAY_KAKAO_NO, USER_EMAIL)
		VALUES(SEQ_PAY_KAKAO.NEXTVAL, #{userEmail})
	</insert>
	
	
	
	
	
	
	
	
	<!--
	<select id="checkReadyInput" parameterType="java.util.list" resultType="java.util.HashMap">    
		SELECT COUNT(CART_NO), SUM(PDT_OPTION_PRICE)
		FROM TB_CART
		JOIN TB_PRODUCT_OPTION USING(PDT_OPTION_NO)
		WHERE USER_NO = #{userNo}
		AND CART_NO = IN
	</select>
	 -->
	
	
	
	
	
	
	
</mapper>